# The directory /basketball-hoops/basketball-hoops/data/raw is intended for storing the original raw data files scraped from Basketball Reference. Since this is a directory, it will be intentionally left blank. 

# /basketball-hoops/basketball-hoops/data/raw
# This directory is intentionally left blank.